# o index.html é o arquivo princial de entrada para o navegador

# todo o corpo do html é delimitado pela tag <html></html>
# isso fará com o que estiver de fora não seja visível ?? Nop

# A tag html é distribuida de duas formas, Head, Body and Script
# em head fica toda a informação dada pela pagina ao buscador(metadados)
# em body fica propriamente o corpo do site

# a tag button necessariamente esta conectada com um back end(JS or PHP)
-> a tag button é o começo de uma pagina dinamica 

classname só é utilizada no ambito do typescript ??sim

# a tag meta exibe metadados para o buscador, por ela é que o google identica tanto o conteudo 
# do site como possiveis configurações para o mesmo

# Normalmente o arquivo css fica separado do arquivo HTML mas é possivel incorporar ele por meio da div Style

# A tag aside é Normalmente utilizada para deixar os index visiveis para o leitor
# ela é deslocada para a esquerda ou direta com CSS 

# um sessão reune um conteudo generico mas que tenham relação entre si, é usado para facilitar a divisão e entendimento do layout

# id em css é identificado pelo #

# hr cria um linha de ponta a ponta do site